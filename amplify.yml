version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use
        - npm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - .cache/**/*
      - node_modules/**/*
